server.modules = (
  "mod_cgi"
)

server.document-root = "/jffs/addons/mihomo/webui/www"
server.bind = "0.0.0.0"
server.port = 5581
server.pid-file = "/opt/root/mihomo/run/mihomo-web.pid"
server.username = "nobody"
server.groupname = "nobody"
index-file.names = ( "index.html" )

$HTTP["url"] =~ "^/cgi-bin/" {
    cgi.assign = ( "" => "/bin/sh" )
}
